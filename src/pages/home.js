const Home = () => {
    return ( 
        <div className="container">
            <h1 className="text-primary">React JS</h1>
            <h2>Describing the UI</h2>
            <details>React is a JavaScript library for rendering user interfaces (UI). UI is built from small units like buttons, text, and images. React lets you combine them into reusable, nestable components. From web sites to phone apps, everything on the screen can be broken down into components. In this chapter, you’ll learn to create, customize, and conditionally display React components.z</details>
        </div>
    )
}

export default Home